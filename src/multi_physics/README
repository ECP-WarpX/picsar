The PICSAR MULTIPHYSICS library is a header only libray.
Unit tests can be compiled and run with the following commands:

$ mkdir build
$ cd build/
$ cmake ../.
$ make
$ make test

QED_tests/validation contains a Jupyter notebook with details
concerning the physical models used in the library.